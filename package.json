{
  "name": "402index",
  "version": "0.1.0",
  "description": "Protocol-agnostic directory of paid APIs (L402 + x402) for AI agents",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "poll": "node src/poll.js",
    "healthcheck": "node src/healthcheck.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0",
    "js-yaml": "^4.1.0",
    "node-fetch": "^3.3.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
